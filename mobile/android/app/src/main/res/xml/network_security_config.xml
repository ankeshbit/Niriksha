<?xml version="1.0" encoding="utf-8"?>
<!--
  AUDIT-SEC-01: Network security configuration for NiriKsha.
  Production: HTTPS only (cleartext traffic blocked globally).
  Development: Cleartext allowed only for localhost and Android emulator (10.0.2.2).
-->
<network-security-config>
    <!-- Default: block cleartext traffic (production safety) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Development-only: allow cleartext for emulator and localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
